# Plan for #5: Enable the metrics endpoint in the deployment

## Goal
Enable the existing metrics listener in the deployed pod and declare its second container port as `metrics`. Document a safe way to reach that listener without changing the application or exposing an additional Kubernetes Service port.

## Dependencies
- **confirmed** — change: yes — `src/config.ts` (`loadConfig`) already requires `METRICS_PORT` when `METRICS_ENABLED=true`; no application code change is needed.
- **confirmed** — change: yes — `k8s/configmap.yaml` currently disables metrics and omits `METRICS_PORT`; set it to true and 9100 (matching the existing config unit test).
- **confirmed** — change: yes — `k8s/deployment.yaml` (`Deployment` container ports) currently declares only HTTP; add container port 9100 named `metrics`.
- **confirmed** — change: yes — `README.md` documents metrics configuration but not how to reach a deployed listener; add a port-forward example and endpoint/output description.
- **confirmed** — change: no — `src/server.ts` (`metrics` HTTP listener) already serves plain-text metrics on the configured port.
- **confirmed** — change: no — `k8s/service.yaml`; the ticket asks for a named pod container port, not a Service port; README access will use `kubectl port-forward`.
- **confirmed** — change: no — `test/`, `e2e/`; no application behavior changes are planned; deployment proof is the required CI rollout/smoke pipeline.

## Tests
- No source or test changes planned. Run `npm run verify` for regression coverage and rely on the pipeline deployment/smoke/browser checks to validate the manifest rollout.
- Inspect rendered manifests/diff and pipeline summary to confirm the named `metrics` port and healthy deployment.

## Environment contract
Existing variables only: set `METRICS_ENABLED=true` and provide the already-required conditional `METRICS_PORT=9100` in the ConfigMap. Keep the README environment table consistent; `src/config.ts` already validates the port.

## Risks
- Enabling metrics without `METRICS_PORT` or declaring a different port would prevent startup or make the named container port misleading.
- CI can fail during deploy/rollout, stability, smoke, or browser checks; inspect diagnostics before changing anything.
- The Service will not expose metrics; the documented port-forward is for reaching the pod listener as required here.

## Open questions
None. Use port 9100, matching the existing configuration test; keep metrics on the pod only and document `kubectl port-forward deployment/sandbox-app 9100:9100`, then `http://localhost:9100/`.

## Decisions
- No user decisions needed; proceed with the above interpretation.
