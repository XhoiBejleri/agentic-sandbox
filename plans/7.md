# Plan for #7: Add readiness and liveness probes, run two replicas

## Goal
Run two app pods and have Kubernetes use the existing health endpoint to determine when each pod may receive traffic and when an unhealthy process should be restarted. Keep the HTTP Service and application behavior unchanged.

## Dependencies
- **confirmed** — change: yes — `k8s/deployment.yaml` (`Deployment.spec`) currently has one replica and no probes; set two replicas and add readiness/liveness HTTP probes on `/health` using the existing named `http` port.
- **confirmed** — change: no — `src/app.ts` (`GET /health`) already returns HTTP 200 with `{ status: "ok" }`.
- **confirmed** — change: no — `k8s/service.yaml` already targets port `http`; readiness will keep unready pods out of Service endpoints.
- **confirmed** — change: no — `.github/workflows/ci.yml` already waits for rollout and checks all selected pods are Ready without restarts, then runs smoke and browser checks.
- **confirmed** — change: no — `k8s/configmap.yaml` and `src/config.ts`; no configuration changes are required.
- **confirmed** — change: no — `test/`, `e2e/`; this is a Kubernetes deployment change; CI verifies rollout, both pods, smoke, and browser behavior.

## Tests
- No application test changes planned. Run `npm run verify` for regression coverage; use the pipeline's rollout, stability check, smoke test, and browser test to verify the manifest in kind.

## Environment contract
No variables added or changed. Existing config and ConfigMap remain in sync.

## Risks
- A wrong probe path or port will leave pods unready and fail rollout; use `/health` on port `http` (container port 3000).
- Two replicas must both become Ready and remain stable; resource requests/limits remain unchanged.
- CI failures may occur in rollout, stability, smoke, or browser; inspect the failing step and diagnostics before any fix.

## Open questions
None. Use a short readiness startup delay (2s) and liveness delay (10s), with HTTP checks on `/health`, port `http`.

## Decisions
- No user decisions needed; proceed with this probe configuration.
